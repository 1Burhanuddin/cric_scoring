# Placeholder app logo

`ic_app_logo.png` (512×512) is a **temporary placeholder** generated for CricHeros
during Phase 1. It uses the brand red `#E21C28` with a teal `#18958F` disc.

Replace it before release with the final brand artwork, then regenerate the
launcher icons (e.g. via `flutter_launcher_icons`) so the Android `mipmap`
`ic_launcher` assets and this in-app logo stay in sync.
