import 'package:flutter/material.dart';

/// CricHeros brand constants.
///
/// Central place for brand colours and copy used across the app. The same
/// palette is wired into the Material/Cupertino themes in
/// `theme/colors.dart`; use these constants for one-off brand surfaces
/// (splash, share cards, marketing UI, etc.).
class CricherosBrand {
  CricherosBrand._();

  static const Color primaryRed = Color(0xFFE21C28);
  static const Color darkRed = Color(0xFFB01020);
  static const Color teal = Color(0xFF18958F);
  static const Color darkTeal = Color(0xFF0F6B67);
  static const Color darkBg = Color(0xFF1A1A2E);
  static const Color darkSurface = Color(0xFF16213E);

  static const String appName = 'CricHeros';
  static const String tagline = 'Your Cricket Matters';
}
